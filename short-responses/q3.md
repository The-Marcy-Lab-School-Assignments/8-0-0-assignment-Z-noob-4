# Question

`app.get(endpoint, controller)` takes in a `controller` as the second argument.

What is a controller?

# Answer
Controllers are components in server-side applications that manage the logic behind each endpoint, deciding how the server should handle requests and may interacting with databases, processing data, and then sens responses to clients.

# Teacher Feedback

X/3 points